{
  "name": "wad",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local read-only WhatsApp archive",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "start": "node src/main.ts",
    "test": "node --test \"test/**/*.test.ts\"",
    "replay": "node src/main.ts --replay-fixtures"
  },
  "dependencies": {
    "baileys": "7.0.0-rc14"
  }
}
